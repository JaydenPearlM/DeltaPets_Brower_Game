project_id = "uhdpwgybrcizligqypzb"

# Supabase local dev configuration.
# This project ref matches supabase/.temp/project-ref in the uploaded ZIP.
# Run `supabase init` if you need the full generated local stack config.